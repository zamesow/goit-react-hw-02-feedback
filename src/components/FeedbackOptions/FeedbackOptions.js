import React from 'react';

function FeedbackOptions() {
  return (
    <ul className="btnBlock">
      {/* <li key={item}>
                <button onClick={e => onLeaveFeedback(item)} className="btn">
                    {item}{' '}
                    </button>
            </li> */}
    </ul>
  );
}

export default FeedbackOptions;
